# 一个月速成计划：研究生科研资料 Agent

## 一、总目标

在 **2026 年 5 月底前**，完成一个可以写进简历、可以投“大模型应用开发实习”的项目。

项目名称：

```
研究生科研资料 Agent
Research Agent Assistant
```

一句话介绍：

```
一个面向研究生学习和科研场景的 RAG + Agent 应用，支持上传论文、课程资料、实验室文档，基于资料进行问答，返回引用来源，并能调用 CSV 查询工具和论文总结工具。
```

最终成果：

```
1. 一个 GitHub 仓库
2. 一个能运行的本地 Demo
3. 一个 Streamlit 页面
4. 一个完整 README
5. 一段简历项目描述
6. 一套面试可讲的技术点
```

------

# 二、项目最终功能

项目最终要支持这些功能：

```
1. 调用大模型 API 进行问答
2. 读取 txt 文档
3. 读取 PDF 文档
4. 文本切分 chunk
5. 使用 embedding 生成向量
6. 使用 Chroma 存储向量
7. 根据用户问题检索相关 chunk
8. 基于检索内容生成回答
9. 回答时返回引用来源
10. 读取 CSV 表格并查询任务信息
11. 使用 LangGraph 做简单任务路由
12. 支持文档问答 / 表格查询 / 论文总结三类任务
13. 用 Streamlit 做交互页面
14. 写 README、Demo、简历项目描述
```

------

# 三、技术栈

优先使用简单、适合实习项目的技术栈。

```
语言：Python
大模型 API：OpenAI / DeepSeek / 通义千问，选一个即可
LLM 框架：LangChain
Agent 编排：LangGraph
向量数据库：Chroma
文档解析：pypdf
表格处理：pandas
前端 Demo：Streamlit
环境管理：venv 或 uv
版本管理：Git + GitHub
```

先不做复杂内容：

```
不做模型训练
不做 LoRA 微调
不做复杂前端
不做多用户系统
不做复杂数据库
不做权限系统
```

------

# 四、项目目录结构

最终目录大概是这样：

```
research-agent-assistant/
  app/
    main.py
    llm.py
    loader.py
    splitter.py
    vectorstore.py
    rag.py
    tools.py
    graph.py
    schemas.py

  data/
    papers/
    docs/
    tasks.csv

  vectorstore/

  demo/
    streamlit_app.py

  eval_cases.md
  README.md
  requirements.txt
  .env.example
  .gitignore
```

每个文件的作用：

```
main.py          命令行入口
llm.py           调用大模型 API
loader.py        读取 txt / PDF
splitter.py      文本切分
vectorstore.py   Chroma 向量库相关代码
rag.py           RAG 问答逻辑
tools.py         CSV 查询、论文总结等工具
graph.py         LangGraph 流程编排
schemas.py       数据结构
streamlit_app.py Streamlit 页面
```

------

# 五、阶段计划

## 阶段 1：先跑通最小 LLM 应用

时间：

```
Day 1 - Day 4
```

目标：

```
能在命令行里输入问题，调用大模型 API，并得到回答。
```

### Day 1：创建项目

完成：

```
1. 新建 GitHub 仓库 research-agent-assistant
2. 创建本地项目文件夹
3. 创建 Python 虚拟环境
4. 新建 requirements.txt
5. 新建 .env.example
6. 新建 README.md
7. 新建 app/main.py
```

当天验收标准：

```
能运行 python app/main.py
终端输出 hello research agent
```

------

### Day 2：调用大模型 API

完成：

```
1. 在 .env 中配置 API key
2. 写 app/llm.py
3. 写一个 call_llm(prompt) 函数
4. 在 main.py 中调用模型
```

当天验收标准：

```
命令行输入：什么是 RAG？
模型能返回回答。
```

------

### Day 3：读取 txt 文档

完成：

```
1. 在 data/docs/ 下放一个 test.txt
2. 写 loader.py
3. 实现 load_txt(path)
4. 把文档内容塞进 prompt
5. 让模型基于文档回答
```

当天验收标准：

```
用户问文档相关问题时，模型能根据 test.txt 回答。
```

------

### Day 4：整理代码结构

完成：

```
1. 把 LLM 调用放进 llm.py
2. 把文件读取放进 loader.py
3. main.py 只负责主流程
4. README 写上当前进度
```

当天验收标准：

```
代码不是全塞在一个文件里。
```

------

## 阶段 2：做出 RAG MVP

时间：

```
Day 5 - Day 10
```

目标：

```
实现：文档读取 → 文本切分 → 向量化 → 检索 → 基于上下文回答。
```

------

### Day 5：文本切分

完成：

```
1. 新建 splitter.py
2. 使用 LangChain text splitter
3. 把长文档切成 chunks
4. 打印每个 chunk 的长度和内容片段
```

需要理解：

```
chunk_size 是什么
chunk_overlap 是什么
为什么不能把整篇文章直接塞进 prompt
```

当天验收标准：

```
一个长文档能被切成多个 chunk。
```

------

### Day 6：接入 Chroma

完成：

```
1. 安装 chromadb
2. 新建 vectorstore.py
3. 把 chunks 存入 Chroma
4. 本地生成 vectorstore 文件夹
```

当天验收标准：

```
文档 chunk 可以成功写入向量数据库。
```

------

### Day 7：实现检索

完成：

```
1. 用户输入问题
2. 从 Chroma 检索 top_k 个相关 chunk
3. 打印检索结果
```

当天验收标准：

```
问一个问题，能返回 3 个相关文档片段。
```

------

### Day 8：组合成 RAG

完成：

```
1. 新建 rag.py
2. 实现 ask_with_rag(question)
3. 流程：问题 → 检索 chunks → 拼 prompt → 调模型 → 返回答案
```

当天验收标准：

```
用户问文档相关问题，模型基于检索片段回答。
```

------

### Day 9：支持 PDF

完成：

```
1. 安装 pypdf
2. loader.py 支持 load_pdf(path)
3. 能读取 PDF 每一页文本
4. 每个 chunk 保留来源文件和页码
```

当天验收标准：

```
放入一篇 PDF，系统可以基于 PDF 回答问题。
```

------

### Day 10：答案带引用

完成：

```
1. 每个 chunk 保存 metadata
2. metadata 包括 source、page、chunk_id
3. 回答时返回引用来源
```

回答格式：

```
回答：
……

引用来源：
1. xxx.pdf，第 3 页
2. xxx.pdf，第 5 页
```

当天验收标准：

```
每次回答都能看到引用来源。
```

------

## 阶段 3：加入 Agent 能力

时间：

```
Day 11 - Day 15
```

目标：

```
让系统不只是普通 RAG，而是可以根据用户问题选择不同工具。
```

------

### Day 11：CSV 查询工具

准备文件：

```
data/tasks.csv
```

内容示例：

```
name,task,deadline,status
张三,RAG论文总结,2026-05-20,未完成
李四,LangGraph实验,2026-05-22,进行中
王五,项目README整理,2026-05-25,未完成
```

完成：

```
1. 新建 tools.py
2. 使用 pandas 读取 tasks.csv
3. 实现 query_tasks(name=None, status=None)
```

当天验收标准：

```
用户问“张三还有什么任务没完成？”，系统可以查询 CSV 并回答。
```

------

### Day 12：论文总结工具

完成：

```
1. 实现 summarize_paper()
2. 输出固定 Markdown 结构
```

输出格式：

```
# 论文总结

## 研究背景

## 方法

## 主要贡献

## 局限性

## 可借鉴点
```

当天验收标准：

```
用户输入“帮我总结这篇论文”，系统能输出结构化总结。
```

------

### Day 13：LangGraph 路由流程

新建：

```
app/graph.py
```

设计 4 个节点：

```
Router：判断任务类型
Retriever：执行文档检索
ToolExecutor：执行 CSV 查询或总结工具
AnswerWriter：整理最终答案
```

流程：

```
用户问题
  ↓
Router
  ↓
文档问答 / 表格查询 / 论文总结
  ↓
AnswerWriter
  ↓
最终答案
```

当天验收标准：

```
同一个入口可以处理三类问题：
1. 文档问答
2. CSV 查询
3. 论文总结
```

------

### Day 14：失败兜底

完成：

```
1. 检索不到内容时，提示上传相关文档
2. CSV 查不到人时，提示未找到该成员
3. 没有引用时，提示回答可能不可靠
4. API 报错时，返回友好错误信息
```

当天验收标准：

```
项目遇到常见错误不会直接崩溃。
```

------

### Day 15：整理工程结构

完成：

```
1. 删除无用测试代码
2. 补充 requirements.txt
3. 补充 .env.example
4. 检查项目能从头运行
5. README 写一版完整草稿
```

当天验收标准：

```
别人 clone 项目后，按照 README 能跑起来。
```

------

## 阶段 4：做 Demo 和包装

时间：

```
Day 16 - Day 19
```

目标：

```
把项目从“代码能跑”变成“别人能看懂、能体验”。
```

------

### Day 16：Streamlit 页面

新建：

```
demo/streamlit_app.py
```

页面功能：

```
1. 上传 PDF
2. 输入问题
3. 显示回答
4. 显示引用来源
```

当天验收标准：

```
运行 streamlit run demo/streamlit_app.py 后，可以在网页里问答。
```

------

### Day 17：README 完整版

README 必须包括：

```
1. 项目简介
2. 项目背景
3. 技术栈
4. 系统架构
5. 功能展示
6. 安装方式
7. 运行方式
8. 示例问题
9. 项目难点
10. 后续优化
```

架构说明：

```
PDF / TXT / CSV
  ↓
Loader
  ↓
Chunking / Parsing
  ↓
Vector Store / Tools
  ↓
LangGraph Router
  ↓
LLM
  ↓
Answer + Citation
```

当天验收标准：

```
一个陌生人打开 GitHub 后，能看懂你项目做什么。
```

------

### Day 18：录 Demo 或截图

准备 3 个演示问题：

```
1. 这篇论文主要讲了什么？
2. 根据文档，实验室报销流程是什么？
3. 张三还有哪些任务没完成？
```

完成：

```
1. 截图
2. 或录一个 1-2 分钟视频
3. 放到 README 里
```

当天验收标准：

```
README 里有项目运行效果。
```

------

### Day 19：准备测试样例

新建：

```
eval_cases.md
```

内容格式：

```
问题：
期望检索内容：
实际回答：
是否有引用：
是否正确：
备注：
```

至少准备 10 条。

当天验收标准：

```
你能说明自己不是随便测了一下，而是有基本评估意识。
```

------

## 阶段 5：简历与投递

时间：

```
Day 20 - Day 24
```

目标：

```
把项目写进简历，然后开始投递。
```

------

### Day 20：写简历项目描述

简历项目可以这样写：

```
研究生科研资料 Agent | Python, LangChain, LangGraph, Chroma, Streamlit

- 基于 LangChain + Chroma 实现科研资料 RAG 问答系统，支持 PDF 解析、文本切分、向量检索与上下文增强生成。
- 设计答案溯源模块，返回引用片段、来源文件和页码，降低大模型幻觉风险。
- 基于 LangGraph 构建 Router-Retriever-ToolExecutor-AnswerWriter 多节点流程，实现文档问答、CSV 查询和论文总结等任务分发。
- 使用 Streamlit 搭建交互式 Demo，支持用户上传资料并进行问答；整理 10+ 条 QA 测试样例，对 chunk_size 和 top_k 参数进行对比。
```

当天验收标准：

```
简历里有一个完整、可讲的大模型应用项目。
```

------

### Day 21：准备面试问题

必须会讲：

```
1. RAG 的完整流程是什么？
2. 为什么要切 chunk？
3. chunk_size 和 chunk_overlap 怎么选？
4. embedding 是什么？
5. 向量检索为什么可能检索错？
6. 如何减少大模型幻觉？
7. 为什么要返回引用？
8. LangGraph 的 node、edge、state 是什么？
9. Router 节点怎么判断任务类型？
10. 工具调用怎么设计？
11. 项目里最难的问题是什么？
12. 你怎么评估回答质量？
```

当天验收标准：

```
每个问题都能回答 1-2 分钟。
```

------

### Day 22：开始投递

投递关键词：

```
大模型应用开发实习
LLM 应用开发实习
RAG 实习
Agent 实习
AIGC 后端实习
AI 应用工程实习
LangChain 实习
LangGraph 实习
Prompt Engineering 实习
```

投递策略：

```
1. 不只投大厂
2. 创业公司也投
3. AI 应用公司重点投
4. 实验室项目也可以投
5. 每天投 10-20 个
```

当天验收标准：

```
投出第一批简历。
```

------

### Day 23 - Day 24：边投边改

根据 JD 修改简历关键词。

如果 JD 写：

```
RAG / Agent / 工作流编排 / FastAPI / LangChain / 向量数据库
```

你的简历里就要有：

```
RAG
Agent
LangGraph
Chroma
LangChain
工具调用
答案溯源
```

当天验收标准：

```
简历不是一版投到底，而是根据岗位微调。
```

------

# 六、每天学习比例

每天如果有 4 小时：

```
2.5 小时：写项目
40 分钟：看 Hello-Agents
20 分钟：看 learn-claude-code
20 分钟：写 README / 记录问题 / commit
```

每天如果只有 2 小时：

```
90 分钟：写项目
20 分钟：看教程
10 分钟：写记录 / commit
```

原则：

```
写项目优先
教程只看和项目有关的
每天必须有 commit
不要连续两天只看课不写代码
```

------

# 七、Hello-Agents 学习范围

只看这些：

```
1. Agent 基础概念
2. ReAct
3. RAG
4. LangGraph
5. 工具调用
6. 上下文工程
7. Agent 评估
```

暂时不看：

```
1. 复杂多 Agent 仿真
2. Agentic RL
3. 太复杂的协议
4. 赛博小镇类项目
```

------

# 八、learn-claude-code 学习范围

只看前面思想部分：

```
1. agent loop
2. 工具注册
3. 任务规划
4. 子任务
5. 上下文压缩
6. 权限边界
```

学它的目的不是复刻 Claude Code，而是把思想用到自己的项目里。

可以借鉴这些设计：

```
1. tools.py 做工具注册表
2. graph.py 做任务路由
3. 回答前做引用检查
4. 长上下文只保留相关 chunk
5. 对危险操作加确认机制
```

------

# 九、项目最低可投递版本

如果时间不够，最低做到这个版本也可以开始投：

```
1. 支持 PDF 问答
2. 支持 RAG 检索
3. 支持答案引用
4. 有 Streamlit 页面
5. 有 README
6. 有简历描述
```

这是最低版本。

更好的版本是：

```
1. 支持 PDF / TXT
2. 支持 RAG
3. 支持答案引用
4. 支持 CSV 查询工具
5. 支持论文总结工具
6. 使用 LangGraph 做任务路由
7. 有 Streamlit 页面
8. 有 README
9. 有测试样例
10. 有 Demo 视频或截图
```

------

# 十、简历最终描述

可以直接保存这段：

```
研究生科研资料 Agent | Python, LangChain, LangGraph, Chroma, Streamlit

- 基于 LangChain + Chroma 实现科研资料 RAG 问答系统，支持 PDF / TXT 文档解析、文本切分、向量检索与上下文增强生成。
- 设计答案溯源模块，返回引用片段、来源文件和页码，提升回答可验证性并降低大模型幻觉风险。
- 基于 LangGraph 构建 Router-Retriever-ToolExecutor-AnswerWriter 多节点流程，实现文档问答、CSV 查询和论文总结等任务分发。
- 封装 CSV 查询、论文总结等工具函数，实现简单工具调用能力；使用 Streamlit 搭建交互式 Demo。
- 整理 10+ 条 QA 测试样例，对 chunk_size、top_k 等参数进行对比，分析检索质量对回答效果的影响。
```

------

# 十一、面试讲法

面试官问：

```
你这个项目是做什么的？
```

回答：

```
我做的是一个面向研究生科研场景的 RAG + Agent 应用。用户可以上传论文、课程资料或实验室文档，系统会对文档进行解析、切分、向量化存储，然后根据用户问题检索相关片段，并调用大模型生成回答。为了降低幻觉，我在回答中加入了引用来源。同时，我还用 LangGraph 做了一个简单的任务路由流程，可以根据问题类型分发到文档问答、CSV 查询或论文总结工具。
```

面试官问：

```
你为什么用 RAG？
```

回答：

```
因为直接让大模型回答时，它不知道用户上传的私有文档内容，也容易产生幻觉。RAG 的作用是先从本地知识库中检索和问题相关的片段，再把这些片段作为上下文交给模型回答，这样可以让回答更贴近文档内容，也方便返回引用来源。
```

面试官问：

```
你为什么用 LangGraph？
```

回答：

```
普通 RAG 是固定流程，但我的项目里有多种任务，比如文档问答、CSV 查询和论文总结。LangGraph 可以把这些步骤拆成节点，并通过 Router 节点根据用户问题选择不同路径。这样流程更清晰，也方便后续扩展新的工具。
```

------

# 十二、每天执行模板

每天开始前问自己：

```
今天的最小目标是什么？
```

每天结束前检查：

```
1. 今天代码有没有跑起来？
2. 今天有没有 commit？
3. README 有没有更新一点？
4. 明天下一步是什么？
```

每天记录：

```
今天完成：
遇到的问题：
解决方式：
明天目标：
```

------

# 十三、最重要原则

整个过程中，只遵守这几个原则：

```
1. 不追求完美，先跑通
2. 不贪多，只做一个主项目
3. 不连续看课，必须写代码
4. 不做复杂算法，专注应用开发
5. 不照抄项目，要改成自己的场景
6. 每天都 commit
7. 5 月底先投起来，边投边改
```



# 小的调整

``` 你现在的「研究生科研资料 Agent」计划，本质上已经和她的路线高度一致：不是先死磕大模型原理，而是做一个能展示、能写简历、能面试讲清楚的 RAG + Agent 应用；你计划里也已经包含 PDF/TXT 解析、chunk 切分、Chroma 向量库、引用来源、CSV 工具、LangGraph 路由、Streamlit 页面、README、简历描述和面试问题准备 。所以方向不用改。 真正要改的是优先级和工程化程度。 结论：你的计划可以保留，但要调整成“先投递版，再增强版” 你原计划有点像“把项目做完整再投”。我建议改成： 先尽快做出最低可投递版本： PDF 上传 / 读取 文本切分 Embedding Chroma 检索 RAG 问答 引用来源 Streamlit 页面 README 简历项目描述 做到这里就可以开始投，不要等 LangGraph、CSV 工具、论文总结、测试样例全部做完。 然后再补增强功能： SQLite 元数据存储 CSV 查询工具 论文总结工具 LangGraph Router 混合检索 FastAPI 接口 测试样例 Demo 视频 也就是说，你的计划不是错，而是应该从： 完整做完 → 再投 调整成： MVP 做到能展示 → 立刻开始投 → 边投边补工程化功能 你最该借鉴她的 4 点 第一，项目优先，不要等基础补完。 她的经验里最值得借鉴的是：低基础也可以边做边补。你现在也不需要等 Python、后端、算法、大模型原理都学完才开始。你的主线就应该是每天推进项目。 第二，项目要能讲清楚，不只是能跑。 你现在的计划已经有 README、简历描述、面试问题准备，这是对的。但你要从第一周就开始写： 我为什么这样切 chunk？ 为什么用 Chroma？ metadata 存了什么？ 引用来源怎么返回？ 检索不到怎么办？ 这个项目和普通 ChatPDF 有什么区别？ 这些内容以后就是面试答案。 第三，RAG 项目不要只写“实现了 RAG”。 她的经验里提到文件上传、SQLite、Qdrant、混合索引，这一点你可以借鉴。你现在计划里有 Chroma，但还没有明显的 SQLite 元数据层。建议你加一个很小的 SQLite 模块，用来存： document_id filename file_type upload_time chunk_id page chunk_text vector_id 这样面试时你就能讲“文档元数据和向量存储是分开的”，比只说“我用了向量库”更像工程项目。 第四，AI 编程可以用，但必须看懂。 你可以用 ChatGPT / Cursor / Claude 帮你写代码，但每次生成代码后都要做三件事： 1. 自己跑通 2. 自己改一个小功能 3. README 里记录这段代码的作用 否则面试官一问实现细节，很容易露馅。 我建议你具体这样调整计划 调整 1：把 Chroma 保留，不急着换 Qdrant 她用了 Qdrant，不代表你必须换。你当前计划用 Chroma，更适合快速做 MVP。 现阶段不要纠结： Chroma vs Qdrant LangChain vs LlamaIndex OpenAI vs DeepSeek 先做出来。等项目能跑之后，如果还有时间，再把 README 里写成： 当前使用 Chroma 作为本地向量库，后续可替换为 Qdrant 以支持更强的服务化部署和过滤查询。 这样就够了。 调整 2：加 SQLite，但只做轻量版 你不需要一开始做复杂数据库。只加一个 metadata.db 就行。 新增文件： app/db.py 存这些信息： documents 表：文件名、类型、上传时间 chunks 表：chunk_id、document_id、page、text、vector_id 这会让你的项目从“教程型 RAG”变成“有工程结构的 RAG”。 调整 3：LangGraph 放后面，不要太早上 你原计划 Day 13 做 LangGraph 路由，这个顺序可以，但前提是 Day 10 的 RAG 必须真的跑通。 不要一开始就纠结 Agent 框架。你的优先级应该是： RAG 问答稳定 > 引用来源正确 > 页面能演示 > 再加 LangGraph 如果 RAG 本身不稳定，LangGraph 只是包装。 调整 4：增加一个 FastAPI 可选接口 你现在计划主要是命令行 + Streamlit。对于“大模型应用开发 / AI 后端实习”，建议后期加一个简单 FastAPI： POST /upload POST /ask GET /documents 不需要复杂前端。 这样你的项目可以同时讲： 我做了一个 Streamlit Demo 方便展示； 同时也封装了 FastAPI 接口，方便后续接入真实业务系统。 这个会比单纯 Streamlit 更像“应用开发实习项目”。 你现在的新路线可以改成这样 第一阶段：先做可投递 MVP 目标只有一个：PDF RAG 问答能跑起来。 1. 调用 LLM 2. 读取 PDF 3. chunk 切分 4. embedding 5. Chroma 存储 6. 检索 top_k 7. 生成回答 8. 返回引用来源 9. Streamlit 上传和问答 10. README 写清楚 做到这里就可以开始投。 第二阶段：工程化增强 1. 加 SQLite 存文档和 chunk 元数据 2. 加错误处理：无文档、无检索结果、API 报错 3. 加 eval_cases.md 4. 对比 chunk_size / top_k 5. README 写技术取舍 第三阶段：Agent 化增强 1. 加 CSV 查询工具 2. 加论文总结工具 3. 用 LangGraph 做 Router 4. 统一入口处理：文档问答 / 表格查询 / 论文总结 第四阶段：求职包装 1. 简历项目描述 2. Demo 截图或视频 3. 面试问答 4. 针对 JD 微调关键词 5. 开始投递，边投边改 我的最终建议 她的经验可以借鉴，但你不要追她的完整技术栈。 你要借鉴的是： 低基础也要先做项目 项目要能跑 项目要能讲清楚 RAG 要做出工程细节 简历包装要像真实业务 边投边改，不等完美 你不一定要照搬： Qdrant MCP A2A Web3 复杂 Agent 复杂多工具系统 你当前最稳的路线是： 研究生科研资料 Agent = PDF RAG + 引用来源 + Streamlit + SQLite 元数据 + LangGraph 简单路由 这已经足够作为一个“大模型应用开发实习”的主项目。现在最重要的不是继续改计划，而是先把第一版跑通。 所以我原来的20天的每日计划不用修改吗 Thought for a couple of seconds 要修改，但不是大改。 你的原来 20 天计划方向是对的，核心项目也不用换；但建议把它从“按功能完整推进”改成“先做可投递 MVP，再做增强项”。 也就是： 原计划不用推翻 但要调整优先级 最需要改的地方 你原计划里 Day 1-10 做 RAG，Day 11-15 加 Agent，Day 16-19 做 Demo 和包装，Day 20 后写简历投递 。 我建议改成： Day 1-10：保留，重点跑通 RAG MVP Day 11-13：提前做 Streamlit + README + 简历描述 Day 14 开始：可以投递 Day 14-20：边投边补 LangGraph、CSV、论文总结、测试样例 为什么要这样改 因为你现在最重要的不是“20 天后做出完整项目”，而是： 尽快拥有一个能展示、能写简历、能投递的版本 你原计划把简历和投递放在 Day 20 以后，这有点晚。更好的策略是： 第 10-13 天做出最低可投递版本 第 14 天开始投 后面继续增强项目 修改后的 20 天版本 Day 1-4：不改 继续做： 创建项目 调用 LLM API 读取 txt 整理代码结构 这个阶段保持原计划。 Day 5-10：基本不改，但目标更明确 继续做： 文本切分 Chroma 向量库 检索 RAG 问答 PDF 支持 引用来源 这里是你项目的核心。必须优先完成。 Day 11-13：建议改 原计划 Day 11-13 是 CSV 工具、论文总结、LangGraph。 我建议改成： Day 11：Streamlit 页面 Day 12：README 完整版 + Demo 截图 Day 13：简历项目描述 + 面试讲法 原因是： 只要你有 PDF RAG + 引用来源 + 页面 + README + 简历描述，就已经可以开始投了。 Day 14：开始投递 不要等 Day 20。 Day 14 就可以开始投： 大模型应用开发实习 RAG 实习 Agent 实习 AI 后端实习 LLM 应用开发实习 每天少量投递，边投边改。 Day 15-20：做增强功能 这时候再补： CSV 查询工具 论文总结工具 LangGraph Router SQLite 元数据 eval_cases.md chunk_size / top_k 对比 FastAPI 可选接口 这些是加分项，不应该卡住你开始投递。 你现在的新版节奏应该是 前 10 天：做核心 RAG 第 11-13 天：包装成能展示的项目 第 14 天开始：投递 第 15-20 天：边投边增强 Agent / 工程化能力 一句话：原计划保留 70%，但把 Demo、README、简历、投递提前，把 LangGraph/CSV/论文总结后移。 ```

