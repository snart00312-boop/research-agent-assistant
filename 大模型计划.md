# 研究生科研资料 Agent — 详细开发计划

## 项目目标

面向研究生科研场景的 RAG + Agent 应用，从零搭建一个完整的检索增强生成系统。目标是深入理解每层原理、能科学评估系统优劣、能讲清楚每个模块的设计决策。

---

## 技术栈总览

| 层 | 已完成 | 规划引入 |
|----|--------|----------|
| 大模型 API | 智谱 GLM-4.5-Air (OpenAI SDK) | — |
| LLM 框架 | LangChain (Embeddings / Document / FAISS) | — |
| Embedding | 智谱 embedding-3 (1024维) | — |
| 文本切分 | RecursiveCharacterTextSplitter | 多策略对比 (Day 14) |
| 稠密检索 | FAISS | — |
| 稀疏检索 | — | BM25 / rank-bm25 (Day 11) |
| 重排序 | — | Cross-Encoder BAAI/bge-reranker-v2-m3 (Day 12) |
| 文档解析 | txt / PDF / Markdown | — |
| 评估 | — | 手写 Recall@k / MRR / 忠实度 (Day 13) |
| 前端 | 命令行 | Streamlit (Day 15) |

---

## 目标项目结构

```
research-agent-assistant/
  app/
    main.py            # 命令行入口（阶段演进：检索测试 → RAG 问答）
    llm.py             # 大模型 API 调用（智谱 GLM-4.5-Air）
    loader.py          # 文档读取（Day 9: txt → PDF/MD 扩展）
    splitter.py        # 文本切分（RecursiveCharacterTextSplitter）
    vectorstore.py     # 向量库（embedding + FAISS 存取 + 检索）
    rag.py             # RAG 问答（Day 8: 检索结果 → LLM 生成 + 引用）
    query_rewriter.py  # 查询改写（Day 10: LLM 改写用户查询）
    retriever.py       # 混合检索（Day 11: BM25 + FAISS + RRF 融合）
    reranker.py        # 重排序（Day 12: Cross-Encoder 二次精排）
    eval.py            # 评估体系（Day 13: Recall/MRR/忠实度）
    ui.py              # Streamlit 页面（Day 15: 可视化 Demo）
  data/
    docs/              # 待入库的文档（txt/pdf/md）
    eval/              # 评估测试集（Day 13: test_questions.json）
  vectorstore/         # FAISS 索引持久化目录
  requirements.txt
  .env.example
  README.md
```

---

## 第一阶段：基础搭建 ✅

### Day 1 ✅ — 项目初始化

**做什么**：搭建项目骨架。

**验收标准**：
- 创建项目目录结构，创建 venv 虚拟环境
- 安装 python-dotenv / openai 等基础依赖
- 创建 .env.example 和 .gitignore

---

### Day 2 ✅ — 调用大模型 API

**做什么**：接入智谱 GLM-4.5-Air，实现命令行问答。

**验收标准**：
- `llm.py` 封装 OpenAI SDK 兼容调用
- 命令行输入问题 → 打印模型回答
- temperature / max_tokens 可配置

---

### Day 3 ✅ — 基于文档问答

**做什么**：读取 txt 文档，将文档全文拼接进 prompt。

**验收标准**：
- 加载 test.txt，将全文注入 prompt
- 能就文档内容正确回答
- 问文档外内容时回答"文档中没有提到相关内容"

---

### Day 4 ✅ — 代码模块化重构

**做什么**：将硬编码拆分为独立模块。

**验收标准**：
- `llm.py` / `loader.py` / `main.py` 职责分离
- 路径管理从 `__file__` 推导项目根目录
- 每个模块职责单一

---

### Day 5 ✅ — 文本切分

**做什么**：用 RecursiveCharacterTextSplitter 将长文档切分为 chunk。

**验收标准**：
- chunk_size / chunk_overlap 可配置
- 中文分隔符适配（`\n\n` → `\n` → `。` → `，` → ` `）
- `print_chunks()` 展示每个 chunk 的长度和内容预览

---

### Day 6 ✅ — 向量存储

**做什么**：调用智谱 embedding-3 生成向量 + FAISS 持久化。

**验收标准**：
- `ZhipuAIEmbeddings` 实现 LangChain Embeddings 接口
- 批量 embedding（batch_size=64）
- FAISS 索引持久化到本地 `vectorstore/` 目录

---

### Day 7 ✅ — 向量检索

**做什么**：根据用户问题从 FAISS 中检索 top-k 个最相关的 chunk。

**验收标准**：
- `similarity_search_with_score()` 返回带分数的结果列表
- 格式化打印：距离分数、来源文件、chunk_id、内容片段（前 500 字）
- `main.py` 支持交互式检索查询

---

## 第二阶段：RAG 核心能力进行中

### Day 8 ✅ — RAG 问答闭环 + 引用溯源

**做什么**：将检索到的 chunk 喂给 LLM，生成带来源引用的答案。

**为什么**：检索本身不是目的，回答才是。没有闭环的检索只是半成品。引用溯源是 RAG 区别于普通 LLM 问答的核心价值——用户能验证答案来自文档的哪一段，不是模型编的。

**怎么做**：

1. 新增 `app/rag.py`，核心函数：

   - `build_rag_prompt(question, retrieved_docs)` — 构建 prompt 模板：
     ```
     你是一个研究生科研资料助手。请严格根据下面提供的文档片段回答用户问题。
     如果下面的文档片段中没有相关信息，请如实说"文档中没有提到相关内容"，不要编造。

     ## 参考文档片段
     [1] 来源: data/docs/test.txt, chunk_id: 3
     内容: ...

     [2] 来源: data/docs/test.txt, chunk_id: 7
     内容: ...

     ## 用户问题
     {question}

     ## 要求
     1. 基于参考片段回答，不要引入外部知识
     2. 在答案中用 [1] [2] [3] 标注引用来源
     3. 回答末尾列出参考文献

     ## 回答
     ```

   - `generate_answer(question, retrieved_docs)` — 构建 prompt → 调用 LLM → 返回答案
   - `print_answer_with_citations(answer)` — 格式化打印答案和引用

2. 修改 `main.py`：检索 → 生成 → 输出带引用的答案（替代 Day 7 只打印检索结果）

**验收标准**：

| # | 测试 | 预期结果 |
|---|------|----------|
| 1 | 问文档中有明确答案的问题 | 答案正确，答案中附带 `[1]` `[2]` 引用标注 |
| 2 | 问文档中完全没有的内容 | LLM 回答"文档中没有提到相关内容" |
| 3 | 逐条检查引用 | 每个 `[N]` 对应一个真实检索到的 chunk，来源文件和 chunk_id 正确 |

---

### Day 9 ✅ — 多格式文档加载（PDF + Markdown）

**做什么**：扩展 `loader.py`，让系统能加载 PDF 和 Markdown 文档。

**为什么**：研究生的真实场景——论文是 PDF，课程笔记和实验记录是 Markdown。只支持 txt 无法覆盖实际需求。

**怎么做**：

1. 安装依赖：`pip install PyMuPDF`

2. 重构 `loader.py`：
   - `load_document(path) → str` — 统一入口，根据文件后缀自动分发
   - `_load_txt(path)` — 原有逻辑，UTF-8 读取
   - `_load_pdf(path)` — PyMuPDF 逐页提取文本，页间加空行分隔
   - `_load_markdown(path)` — 直接读取原文，保留标题和段落结构

3. 准备测试文件：1 个中文 PDF 论文 + 1 个 Markdown 笔记，验证全流程

**验收标准**：

| # | 测试 | 预期结果 |
|---|------|----------|
| 1 | 加载中文 PDF 论文 | 文本内容正确提取，无乱码，页间有空行分隔 |
| 2 | 加载 Markdown 笔记 | 文本提取正确，标题和正文可区分 |
| 3 | PDF/MD 全流程串联 | PDF → 切分 → embed → FAISS → 检索 → 问答，不报错 |
| 4 | 混合使用 | 同时加载 txt+PDF+MD，入库后检索，各格式 chunk 均可见 |

---

### Day 10 🔲 — 查询改写 (Query Rewriting)

**做什么**：在检索之前，用 LLM 把用户模糊的问题改写成更适合检索的查询。

**为什么**：用户常问"这个理论对吗""为什么这样设计"——这些问法缺少具体关键词，embedding 检索效果差。查询改写把问题扩展成包含关键术语的完整查询，大幅提升召回率。而且实现成本极低——只需一次额外的 LLM 调用。

**怎么做**：

1. 新增 `app/query_rewriter.py`：
   - `rewrite_query(original_question) → str`：
     ```
     prompt = """
     你是一个查询优化助手。用户正在搜索文档库中的相关信息。
     请将用户的问题改写成更具体、包含关键术语的检索查询。
     如果问题本身已经很具体，直接返回原问题。
     只返回改写后的查询，不要加说明文字。

     用户问题：{question}
     改写查询：
     """
     ```
   - `rewrite_query_with_subqueries(question) → list[str]` — 生成 2-3 个子查询，各自检索后合并去重

2. 在 `main.py` 中加入对比模式：同时展示原始 query 和改写 query 各自的检索结果

**验收标准**：

| # | 测试 | 预期结果 |
|---|------|----------|
| 1 | "这个算法收敛快吗？" | 改写为含具体算法名的查询（如"梯度下降 收敛速度 优化算法"） |
| 2 | 同一问题对比检索 | 改写后 top-3 的语义相关度明显优于原始 query |
| 3 | main.py 开关 | 可选择是否启用查询改写，方便后续对比实验 |

---

## 第三阶段：检索质量提升 🔲

### Day 11 🔲 — 混合检索 (Hybrid Search)

**做什么**：融合 BM25 稀疏检索和 FAISS 稠密检索，用 RRF (Reciprocal Rank Fusion) 排序。

**为什么**：

| 检索方式 | 优势 | 劣势 |
|----------|------|------|
| 稠密检索 (FAISS) | 理解语义，能匹配同义词/近义词/相关概念 | 容易漏掉精确关键词 |
| 稀疏检索 (BM25) | 精确关键词匹配，擅于抓住专有名词和术语 | 不理解语义，同义词匹配不到 |
| 混合检索 | 两者互补，两边都命中的排在前面 | 多一次索引构建，略微增加复杂度 |

工业级 RAG 几乎都用混合检索，这是从"教程级"到"工程级"的关键一步。

**怎么做**：

1. 安装依赖：`pip install rank-bm25`

2. 新增 `app/retriever.py`：
   - `class HybridRetriever`：
     - `__init__(chunks, faiss_vectorstore)` — 同时维护 BM25 和 FAISS 两套索引
     - `build_bm25_index(chunks)` — 分词后构建 BM25Okapi
     - `dense_search(query, top_k)` — 调用 FAISS
     - `sparse_search(query, top_k)` — 调用 BM25
     - `hybrid_search(query, top_k)` — 两路检索 + RRF 融合
   - RRF 公式：`score(doc) = 1/(k + rank_faiss) + 1/(k + rank_bm25)`，k=60
   - `compare_search_modes(query)` — 对比稠密/稀疏/混合三种结果

**验收标准**：

| # | 测试 | 预期结果 |
|---|------|----------|
| 1 | 关键词型查询"transformer" | BM25 找到精确匹配；混合后召回比纯稠密更全面 |
| 2 | 语义型查询"如何处理长距离依赖" | FAISS 找到相关语义；混合后仍有好结果 |
| 3 | 对比三种模式 | `compare_search_modes()` 打印每种方法的 top-3，直观看到差异 |
| 4 | RRF 融合验证 | 两边都命中的 chunk 排在纯稠密或纯 BM25 命中的前面 |

---

### Day 12 🔲 — 重排序 (Reranking)

**做什么**：用 Cross-Encoder 模型对混合检索的 top-k 候选进行二次精细排序。

**为什么**：

| 阶段 | 模型类型 | 速度 | 精度 | 用途 |
|------|----------|------|------|------|
| 粗筛 | Bi-encoder (embedding) | 快 | 中 | 从全库筛出 100 个候选 |
| 精排 | Cross-encoder | 慢 | 高 | 对 100 个候选逐一精确打分，取 top-10 |

Bi-encoder 把 query 和 doc 分开编码再算余弦相似度——速度快但丢失交互信息。Cross-encoder 把 query 和 doc 拼接起来共同编码——慢但精度更高。两阶段结合是工业界检索系统的标准做法。

**怎么做**：

1. 安装依赖：`pip install sentence-transformers`

2. 新增 `app/reranker.py`：
   - 使用 `BAAI/bge-reranker-v2-m3`（中文友好的 Cross-Encoder）
   - `rerank(query, docs, top_k) → list[(doc, score)]`：
     - 构建 (query, doc_text) pair 列表
     - 批量 predict 得到相关度分数
     - 按分数降序排序，返回 top_k
   - `compare_rerank(query, top_k)` — 对比 rerank 前后的排序变化

**验收标准**：

| # | 测试 | 预期结果 |
|---|------|----------|
| 1 | 同一个 query 的 top-5 | rerank 前后排序有明显变化 |
| 2 | 人工判断 | rerank 后的排序比 rerank 前更合理（最相关的排在第 1 位） |
| 3 | 耗时记录 | 打印 rerank 耗时（毫秒级），理解精度提升带来的时间成本 |

---

## 第四阶段：评估与优化 🔲

### Day 13 🔲 — 评估体系（手写指标）

**做什么**：构建测试集 + 手写指标计算，建立量化评估能力。

**为什么**：这是整个项目最有含金量的部分。没有评估就没有科学比较——chunk_size 取 200 还是 500？要不要开 rerank？一切靠"感觉"不严谨。手写指标让你理解每个公式的含义，面试时能讲清原理。

**怎么做**：

1. **构建测试集** — `data/eval/test_questions.json`（20-30 条）：
   ```json
   [
     {
       "id": 1,
       "question": "什么是机器学习？",
       "ground_truth_answer": "机器学习是人工智能的一个分支...",
       "relevant_chunk_ids": [3, 5]
     },
     {
       "id": 2,
       "question": "深度学习与传统机器学习的区别是什么？",
       "ground_truth_answer": "深度学习使用多层神经网络...",
       "relevant_chunk_ids": [8, 12, 15]
     }
   ]
   ```

2. **检索端指标** — `evaluate_retrieval()`：
   - **Recall@k**：相关 chunk 被检索到的比例
     - 公式：`|retrieved ∩ relevant| / |relevant|`
     - 含义：要找的文档，找回来了多少
   - **Precision@k**：检索结果中相关 chunk 的比例
     - 公式：`|retrieved ∩ relevant| / k`
     - 含义：返回的结果里，有多少是真正有用的
   - **MRR** (Mean Reciprocal Rank)：第一个相关结果排名的倒数均值
     - 公式：`1/|Q| × Σ 1/rank_i`
     - 含义：正确答案排在多前面

3. **生成端指标** — `evaluate_generation()`：
   - **答案忠实度**：用 LLM 逐句判断生成的答案是否有 chunk 支撑
     - 提示词："请逐句判断下面的 AI 回答：每一句的事实是否都能在参考文档中找到依据？标注出无依据的句子并说明原因。"
   - **答案相关性**：用 LLM 判断答案是否直接回应了问题

4. 新增 `app/eval.py`：
   - `load_test_set(path)` — 加载 JSON 测试集
   - `evaluate_retrieval(...)` — 计算检索端三个指标
   - `evaluate_generation(...)` — 计算生成端两个指标
   - `run_full_eval()` — 跑完整评估，输出报告
   - 跑一遍当前系统，得到基线分数作为后续对比基准

**验收标准**：

| # | 测试 | 预期结果 |
|---|------|----------|
| 1 | 输出评估报告 | 包含 Recall@3 / Precision@3 / MRR / 忠实度 / 相关性五列分数 |
| 2 | 逐条分析 | 能解释测试集中任意一条为什么得分高或低 |
| 3 | 基线确立 | 为 Day 14 消融实验准备好量化对比基准 |

---

### Day 14 🔲 — 索引优化 + 消融实验

**做什么**：用 Day 13 的评估体系，系统对比不同参数配置，找到最优组合。

**为什么**：Day 5 选了 chunk_size=500、overlap=100，但这些数字是拍脑袋的。现在用数据说话，理解每个参数对最终效果的实际影响。消融实验也是科研方法论的核心——控制变量，一次只改一个因素，观察效果变化。

**怎么做**：

1. **设计实验矩阵**：

   | 变量 | 取值 |
   |------|------|
   | chunk_size | 200, 500, 1000 |
   | chunk_overlap | 0, 50, 100, 200 |
   | top_k | 3, 5, 10 |
   | 检索策略 | 纯稠密(FAISS) / 稠密+BM25混合 / 稠密+BM25+Rerank |

2. **写实验脚本** — `eval.py` 中新增 `run_ablation()`：
   - 遍历所有参数组合（3×4×3×3 = 108 组，可先精选 12-18 组典型组合）
   - 每种组合：重新切分 → 重新入库 → 跑完整评估 → 记录分数
   - 输出 Markdown 格式的对比表格

3. **分析总结**：
   - 哪个 chunk_size 最优？为什么？（太小：丢失上下文 → 忠实度下降；太大：检索精度下降 → Recall 下降）
   - overlap 多大合适？为什么？
   - 混合检索 vs 纯稠密提升多少？
   - Rerank 再加持提升多少？性价比如何？

**验收标准**：

| # | 测试 | 预期结果 |
|---|------|----------|
| 1 | 输出完整对比表格 | 每种配置的五个评估指标一目了然 |
| 2 | 找到最优配置 | 能解释为什么这个配置最优（有理论依据 + 数据支撑） |
| 3 | 更新项目默认参数 | `splitter.py` 的默认 chunk_size/overlap 更新为最优值 |
| 4 | 结论记录 | 在 README 中补充一段"参数选择依据" |

---

## 第五阶段：产品化 🔲

### Day 15 🔲 — Streamlit 可视化 Demo

**做什么**：用 Streamlit 搭建一个可在浏览器中使用的完整 RAG 应用。

**为什么**：命令行只能自己用，Streamlit 能演示给导师看、给同学用。UI 交互会暴露命令行注意不到的体验问题（如大文件 chunking 耗时过长需要进度条、检索无结果时需要友好提示）。

**怎么做**：

1. 安装依赖：`pip install streamlit`

2. 新增 `app/ui.py`：

**侧边栏**：
- 文件上传按钮（支持 txt / pdf / md）
- chunk_size / chunk_overlap 滑块
- "入库"按钮 + 进度条（显示 embedding 耗时）
- 开关组：查询改写 / 混合检索 / 重排序（可独立启用/关闭）

**主界面**：
- 对话式问答（聊天框样式，用户输入 → 系统回答）
- 每条回答下方折叠面板展示：
  - 检索到的 top-3 chunk 全文（关键词高亮）
  - 相似度分数
  - 来源文件名和 chunk_id
- 检索过程可视化（可选）：展示从 query 改写 → 两路检索 → RRF 融合 → Rerank 的中间结果

3. 整合 Day 8-14 全部能力，跑通完整链路

**验收标准**：

| # | 测试 | 预期结果 |
|---|------|----------|
| 1 | 上传 PDF → 入库 → 问答 | 浏览器中全流程跑通，得到带 `[N]` 引用的答案 |
| 2 | 检索可视化 | 折叠面板中能看到检索到的片段、分数和来源 |
| 3 | 开关切换 | 查询改写/混合检索/重排序各开关可独立控制并看到结果差异 |
| 4 | 异常处理 | 无文档时提示上传、检索为空时友好提示、API 报错时显示错误信息 |

---

## 学习路径总览

```
第一阶段 (Day 1-7)    基础搭建 ✅
  LLM API → 文档加载 → 文本切分 → Embedding → FAISS → 检索
  ─────────────────────────────────────────────────────────────
  状态：全部完成

第二阶段 (Day 8-10)   RAG 核心能力 🔲
  检索→LLM生成引用答案 → 多格式文档 → 查询改写
  ───────────────────────────────────────────────
  状态：待开始

第三阶段 (Day 11-12)  检索质量提升 🔲
  BM25混合检索 → Cross-Encoder重排序
  ─────────────────────────────────────
  状态：待开始

第四阶段 (Day 13-14)  评估与优化 🔲
  手写评估指标 → 消融实验找最优配置
  ──────────────────────────────────────
  状态：待开始

第五阶段 (Day 15)     产品化 🔲
  Streamlit 可视化 Demo
  ─────────────────────
  状态：待开始
```

---

## 评估指标速查

| 指标 | 公式 | 衡量什么 |
|------|------|----------|
| Recall@k | `|retrieved ∩ relevant| / |relevant|` | 检索找回了多少相关文档 |
| Precision@k | `|retrieved ∩ relevant| / k` | 检索结果中有多少是相关的 |
| MRR | `1/|Q| × Σ 1/rank_i` | 第一个相关结果排在第几位 |
| 答案忠实度 | LLM 逐句 vs 参考片段对比 | 生成的答案是否都有文档支撑 |
| 答案相关性 | LLM 判断 | 答案是否直接回应了问题 |

---

## 依赖清单

```
# 已有
python-dotenv
openai
langchain-core
langchain-community
langchain-text-splitters
faiss-cpu

# Day 9 引入
PyMuPDF

# Day 11 引入
rank-bm25

# Day 12 引入
sentence-transformers

# Day 15 引入
streamlit
```
